# configs/config.yaml
project:
  name: "cart-abandonment-system"

logging:
  level: INFO
  format: "{time:DD-MM-YYYY HH:mm:ss} | {level} | {message}" 

data:
  raw_path: data/raw_data/online_shoppers_intention.csv

llm:
  provider: gemini
  model: models/gemini-flash-lite-latest